import this
import random
import codecs


def generate_reponse(query):
    # todo: dummy
    zen = random.choice(codecs.decode(this.s, "rot_13").strip().split("\n"))
    return f"Random Zen Of Python >> {zen}"
